# ==============================================================================
# AI-GENERATED CODE DISCLAIMER
# This configuration was generated with the assistance of an AI. 
# Please review and test thoroughly before using in a production environment.
# ==============================================================================

name: AI README Auto-Update

on:
  push:
    branches:
      - main
    # Prevent infinite loops if we merge the README update PR itself
    paths-ignore:
      - 'README.md'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Needed to fetch the git commit history for the script

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        working-directory: ./scripts
        run: npm install

      - name: Generate New README with Gemini
        working-directory: ./scripts
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        # Runs from the root of the repo so it can find README.md
        run: node update_readme.js

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "docs: auto-update README based on recent changes"
          title: "🤖 AI Update: Repository Structure & Logs"
          body: "This PR was automatically generated by Gemini. It updates the README to reflect the latest file structure and commit history."
          branch: auto-update-readme
          delete-branch: true
          add-paths: README.md